<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeWhere"
        tools:targetApi="31">
        <activity
            android:name=".mapmodule.GeofencingOnActivity"
            android:exported="false" />
        <activity
            android:name=".mapmodule.GeofencingActivity"
            android:exported="false" />
        <activity
            android:name=".startupmodule.ProfileMainActivity"
            android:exported="false" />
        <activity
            android:name=".sosmodule.SosActivity"
            android:exported="false" />
        <activity
            android:name=".reportmodule.ReportMainActivity"
            android:exported="false" />
        <activity
            android:name=".notificationmodule.NotificationMainActivity"
            android:exported="false" />
        <activity
            android:name=".informationmodule.MainInformationActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".mapmodule.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".startupmodule.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".mapmodule.MyGeofenceBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.gms.location.ACTION_GEOFENCE_TRANSITION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>